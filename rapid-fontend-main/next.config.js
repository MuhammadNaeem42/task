/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["xsgames.co", "placehold.co"],
    formats: ["image/avif", "image/webp"],
  },
};

module.exports = nextConfig;
